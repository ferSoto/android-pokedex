<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Style for pokémon's name in recycler view cells -->
    <style name="PokeNameCell">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textSize">24sp</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for pokémon's number in recycler view cells -->
    <style name="PokeNumberCell">
        <item name="android:gravity">center_vertical</item>
        <item name="android:textColor">@android:color/darker_gray</item>
        <item name="android:textStyle">italic</item>
    </style>

    <!-- Style for recycler view item title in pokémon details -->
    <style name="PokeDetailItemTitle">
        <item name="android:paddingStart">8dp</item>
        <item name="android:paddingEnd">8dp</item>
        <item name="android:paddingTop">4dp</item>
        <item name="android:paddingBottom">4dp</item>
        <item name="android:gravity">top</item>
        <item name="android:textStyle">bold</item>
    </style>

    <!-- Style for recycler view item content in pokémon details -->
    <style name="PokeDetailItemContent" parent="PokeDetailItemTitle">
        <item name="android:textStyle">normal</item>
        <item name="android:singleLine">false</item>
        <item name="android:gravity">center_vertical</item>
    </style>

</resources>
